# master ブランチ更新の概要

このドキュメントは、最新の `master` ブランチから取得してローカルブランチ (`local_main_ver2`) にマージされた変更内容の概要をまとめたものです。

## 🚀 主な新機能 (Features)
1. **OSGi 状態の永続化 (OSGi state persistence)** (#28)
   - Liferay コンテナ再起動時の OSGi バンドルの状態永続化機能が実装されました。関連して検証用のスクリプト (`scripts/verify_osgi_persistence.sh`) も追加されています。
2. **AIアシスト・オーケストレーション機能 (MCP & CLI)** (#31)
   - AI との連携機能がコア部分に実装されました。
   - `ldm_core/handlers/ai.py` や `ldm_core/handlers/mcp.py` などが新規作成されています。

## 🐛 バグ修正 (Bug Fixes)
1. **タグの大文字小文字の区別とCDNフォールバックロジックの解決** (#25)
   - リソース取得の安定性が向上しました。

## 📝 ドキュメントの更新 (Documentation)
1. **外部データベース機能に関するドキュメント追加** (#26)
2. **PRテスト用アーティファクトのドキュメント追加および AI オーケストレーション完了のマーク** (#34)
   - `conductor/PR_MERGE_PLAN.md` などのドキュメントが追加・更新されています。

## ⚙️ CI / 開発環境・その他 (CI & Dependencies)
1. **PR バイナリの GitHub アーティファクトとしてのコンパイル** (#30)
   - プルリクエスト作成時にバイナリがアーティファクトとして生成されるようになりました。
2. **依存関係のアップデート** (#32, #33)
   - `ruff` を 0.15.15 から 0.15.16 へアップデート。
   - `actions/download-artifact` アクションのバージョンアップ。

---
**影響を受けた主なディレクトリ:**
- `ldm_core/handlers/` (AI関連ロジックの新規追加など)
- `docs/` (ガイドとショウケースの拡充)
- `scripts/` (検証用シェルスクリプトの追加)
- GitHub Actions のワークフロー設定ファイル
